env:
  policy_controller:
    controller_config:
      delan:
        pretrained_model: null          # So that the "before" pretrained model doesn't override the newest
        freeze_base: True
        freeze_extrinsics: True
        freeze_residual: True
